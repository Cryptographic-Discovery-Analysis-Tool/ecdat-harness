# harness §5.3: TLS terminates here, not in payment-gateway.
FROM haproxy:3.0-alpine
COPY haproxy.cfg /usr/local/etc/haproxy/haproxy.cfg
# pay-edge.pem is generated by harness/build/generate-pki.sh into
# targets/payments/edge-lb/certs/ (gitignored, H3) and bind-mounted at
# runtime by harness/compose/docker-compose.yml -- never baked into the
# image.
EXPOSE 8443
