# harness §7.2 row PAY-002.
id: PAY-002
tier: A
domain: payments
application: payment-gateway
planted_truth:
  algorithm_family: HMAC
  algorithm: HMAC-SHA256
  parameters: {}   # not specified beyond the algorithm name in harness §7.2/§5.1
  primitive: mac
  crypto_functions: [sign, verify]
  purpose: webhook-integrity
  security_function: true
  reachable: true
  provider: SunJCE
  quantum_tier: not-affected
locations:
  - surface: source
    path: payments/payment-gateway/src/main/java/com/meridian/pay/crypto/WebhookSigner.java
    lines: [1, 17]
expected_observation:
  governed_by: null
  must_be:
    - "algorithm KNOWN (literal Mac.getInstance(\"HmacSHA256\"))"
  must_not:
    - "purpose KNOWN from Semgrep alone -- MAC => integrity is INFERENCE, not observation (harness §7.2)"
declared_context:
  business_criticality: high
  data_class: null   # not specified in harness §7.2 for PAY-002
  data_lifetime_years: 1
  exposure: external
  environment: prod
migration: null   # not specified in harness §7.2 for PAY-002 -- do not invent
misuse_note: null
